'use client';

import React from 'react';

export default function AdminDashboard() {
  // This component is now deprecated - admin sections are handled by individual pages
  // This is kept for backward compatibility but should be replaced with AdminOverview
  return null;
}